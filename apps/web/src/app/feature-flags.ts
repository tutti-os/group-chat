/** Private agent messages (whisper). Re-enable for multiplayer release. */
export const WHISPER_FEATURE_ENABLED = false;

/** Unread badges and read-state tracking. */
export const UNREAD_FEATURE_ENABLED = true;
